# Vision

Murmur 存在,是为了让中文用户在自己的电脑上把语音变成可用的文字。

它服务需要高频语音输入与音频批量转写的中文用户,把他们的声音变成光标处就位的文字、可检索的转录历史和润色后的成稿。

它只拥有一件事:本地优先的中文语音到文字全链路。

## 本地优先是身份,不是卖点

音频与转写数据不出用户的电脑,识别默认在本地完成。

数据不出电脑不只是隐私承诺,它本身就是端侧竞争力。

联网能力只存在于用户主动配置的可选环节(AI 润色),由用户自选提供商、密钥与本地模型。

引入任何默认上传音频的代码路径都违背本项目身份。

## 为中文优化

中文识别精度、中文口头禅清理、中文成稿场景(会议纪要、社交文案)优先于多语言广度。

对英文或其他语言的优化,只有在不动摇中文体验的前提下才接受。

界面保持中英双语,且两种语言描述的能力与现实保持一致。

## 错位竞争与增长

Murmur 当前不与 macOS 和 Windows 系统听写竞争实时性。

它的差异化是文件转录、AI 后处理、完全本地与可自定义模型。

实时流式转录是一个保持开放的战略决策:投入的前提是验证错位定位仍然成立。

已明确放弃的用户(纯实时打字用户)不通过增加复杂度去挽回。

在错位定位确立的窗口期,投入市场与增长工作是正当的,前提是不为流量用户改变定位。

## 文档与现实一致

对外展示的每条命令、每个徽章、每项能力,都必须在当前版本真实可用。

路线图中的未来能力可以宣传,但必须显式标注为未发布,不得伪装成当前可用。

度量口径可以调整,但调整之后不允许沿用旧口径的数字。

## 根因与证据

同一类问题反复出现时,治理它的结构性温床,而不是继续打补丁。

错误信息分层展示:第一层是用户可执行的动作与原因,第二层是原始技术细节。

技术细节层不做翻译,以日志样式原样展示,新增提供商不增加文案成本。

完整错误链路永远保留在日志中,用户可见的分层不删任何信息。

重构前先用特征测试固化现有行为,再动手。

安全修复依赖真实可验证的数据源,上游声称已修复要验证版本是否真的发布。

## 单人可持续

范围的扩张以单人维护者可长期承受为前提。

战略决策记录为 ADR 或等价的可追溯文档。

测试与 CI 门禁是维护者的杠杆而不是负担,门禁只增不减,除非有记录在案的替代方案。

覆盖率保持全量口径,阈值是只升不降的回归地板。

## Scope

Murmur 不是实时听写引擎,不是云端转写服务,不是团队协作平台,不是付费订阅服务。

商业化探索不得以经手用户的音频、转录或 AI 通道为前提。

用户的音频、转录历史与 API 密钥留在用户设备上。

仓库用 CI 门禁(代码规范、类型、测试、打包冒烟)把自己的标准执行在自己身上。

一项变更与愿景对齐,当它强化本地中文语音到文字的链路,或让已有能力更可靠、更诚实、更易单人维护。

一项变更应当被拒绝,当它默认把数据带出用户设备,为覆盖更多用户而稀释中文体验,宣传不可用的能力,或把维护负担推给未来的自己。
